from enum import Enum

BLOCK_SIZE = 1_000
TYPES_AMOUNT = 6

DATA_LOWER_LIMIT = 1_000
DATA_UPPER_LIMIT = DATA_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT - 1

INSTANCE_LOWER_LIMIT = 7_000
ATTRIBUTE_LOWER_LIMIT = INSTANCE_LOWER_LIMIT
ATTRIBUTE_UPPER_LIMIT = ATTRIBUTE_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT - 1
PROCEDURE_LOWER_LIMIT = ATTRIBUTE_UPPER_LIMIT
VAR_LOWER_LIMIT = 13_000
VAR_UPPER_LIMIT = VAR_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT - 1
TEMP_LOWER_LIMIT = VAR_UPPER_LIMIT+1
TEMP_UPPER_LIMIT = TEMP_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT - 1
PROCEDURE_UPPER_LIMIT = TEMP_UPPER_LIMIT
INSTANCE_UPPER_LIMIT = PROCEDURE_UPPER_LIMIT

CTE_LOWER_LIMIT = INSTANCE_UPPER_LIMIT
CTE_UPPER_LIMIT = CTE_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT - 1

ADJUSTED_INT_BEGIN = 0
ADJUSTED_INT_LIMIT = ADJUSTED_INT_BEGIN + BLOCK_SIZE - 1
ADJUSTED_FLOAT_BEGIN = ADJUSTED_INT_LIMIT + 1
ADJUSTED_FLOAT_LIMIT = ADJUSTED_FLOAT_BEGIN + BLOCK_SIZE - 1
ADJUSTED_CHAR_BEGIN = ADJUSTED_FLOAT_LIMIT + 1
ADJUSTED_CHAR_LIMIT = ADJUSTED_CHAR_BEGIN + BLOCK_SIZE + 1
ADJUSTED_BOOL_BEGIN = ADJUSTED_CHAR_LIMIT + 1
ADJUSTED_BOOL_LIMIT = ADJUSTED_BOOL_BEGIN + BLOCK_SIZE + 1
ADJUSTED_INSTANCE_BEGIN = ADJUSTED_BOOL_LIMIT + 1
ADJUSTED_INSTANCE_LIMIT = ADJUSTED_INSTANCE_BEGIN + BLOCK_SIZE - 1
ADJUSTED_POINTER_BEGIN = ADJUSTED_INSTANCE_LIMIT + 1
ADJUSTED_POINTER_LIMIT = ADJUSTED_POINTER_BEGIN + BLOCK_SIZE + 1

class Types(Enum):
  INT = 'int'
  FLOAT = 'float'
  CHAR = 'char'
  BOOL = 'bool'
  CTE_STRING = 'cte_string'
  CLASS = 'class'
  VOID = 'void'
  POINTER = 'pointer'
  READ = 'read'

raw_var_types = (
  'int',
  'float',
  'char',
  'bool',
  'pointer',
)
