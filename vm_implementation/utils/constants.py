BLOCK_SIZE = 1_000
TYPES_AMOUNT = 5
DATA_LOWER_LIMIT = 1_000
DATA_UPPER_LIMIT = DATA_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT-1
INSTANCE_LOWER_LIMIT = 6_000
INSTANCE_UPPER_LIMIT = INSTANCE_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT-1
PROCEDURE_LOWER_LIMIT = 11_000
VAR_LOWER_LIMIT = PROCEDURE_LOWER_LIMIT
VAR_UPPER_LIMIT = PROCEDURE_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT-1
TEMP_LOWER_LIMIT = VAR_UPPER_LIMIT + 1
TEMP_UPPER_LIMIT = TEMP_LOWER_LIMIT + BLOCK_SIZE * TYPES_AMOUNT-1
PROCEDURE_UPPER_LIMIT = TEMP_UPPER_LIMIT
CTE_LOWER_LIMIT = 21_000
CTE_UPPER_LIMIT = CTE_LOWER_LIMIT + BLOCK_SIZE * 5
INT_LOWER_LIMIT = 0
INT_UPPER_LIMIT = INT_LOWER_LIMIT + BLOCK_SIZE-1
FLOAT_LOWER_LIMIT = INT_UPPER_LIMIT + 1
FLOAT_UPPER_LIMIT = FLOAT_LOWER_LIMIT + BLOCK_SIZE-1
CHAR_LOWER_LIMIT = FLOAT_UPPER_LIMIT + 1
CHAR_UPPER_LIMIT = CHAR_LOWER_LIMIT + BLOCK_SIZE-1
BOOL_LOWER_LIMIT = CHAR_UPPER_LIMIT + 1
BOOL_UPPER_LIMIT = BOOL_LOWER_LIMIT + BLOCK_SIZE-1
CLASS_LOWER_LIMIT = BOOL_UPPER_LIMIT + 1
CLASS_UPPER_LIMIT = CLASS_LOWER_LIMIT + BLOCK_SIZE-1
